tasks:
  build:
    command: "go build -o ./bin/locus ./cmd/locus"

  test:
    command: "go test -json ./..."
    source: go-test
    depends: [build]

  vet:
    command: "go vet ./..."
    source: go-vet

  check:
    depends: [test, vet]
