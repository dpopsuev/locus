// handlers imports core, state, and router — maximises cross-edges within the SCC
import { Core } from '../core/index';
import { State } from '../state/index';
import { Router } from '../router/index';
export class Handler { c: Core; s: State; r: Router; }
